import { Typography } from "@mui/material";

const PageHeader = () => {
    return (
        <Typography
        variant="h4"
        component="h1"
        color="primary"
        fontWeight="bold"
        sx={{ mb: 2 }}
      >
        Listado de Asistencia
      </Typography>
    )
}

export default PageHeader