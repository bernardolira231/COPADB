const useSx = () => ({
  root: {
    minWidth: 250,
    backgroundColor: "white",
    borderRadius: 1,
  },

  select: {
    fontSize: "0.9rem",
    "&:hover": {
      backgroundColor: "#e5e5e5",
    },
  },
});

export default useSx;
